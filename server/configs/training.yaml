project:
  name: "TrueFrame AI"
  base_dir: ".."

data:
  dataset_path: "data"
  model_save_path: "models/deepfake_model.pth"
  log_path: "logs/training_log.txt"

training:
  batch_size: 8
  epochs: 15
  learning_rate: 0.001

preprocessing:
  img_size: [224, 224]
